import PartnerPage from "@/components/partner/PartnerPage";

export const metadata = {
  title: "Partner with Us | StrideNex",
  description: "Join the StrideNex Ground-Level Distribution Network. Turn your trust into a revenue stream while enabling better student outcomes.",
};

export default function PartnerWithUs() {
  return <PartnerPage />;
}
